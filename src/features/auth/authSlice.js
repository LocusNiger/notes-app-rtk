import { createSlice } from "@reduxjs/toolkit";

const initialState = [
  {
    email: null,
    password,
  },
];

export const authSlice = createSlice({
  name: "auth",
  initialState,
  reducers: {},
});
